name: Codecov

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Coveralls GitHub Action
      uses: coverallsapp/github-action@v1.1.2
      env:
        CODECOV_TOKEN: ${{ secrets.COVERALLS_REPO_TOKEN }}
